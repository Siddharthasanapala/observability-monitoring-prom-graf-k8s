apiVersion: v1
kind: Service
metadata:
  name: observability-sample-app
  namespace: demo-app
  labels:
    app.kubernetes.io/name: observability-sample-app
spec:
  selector:
    app.kubernetes.io/name: observability-sample-app
  ports:
    - name: http
      port: 8080
      targetPort: http
